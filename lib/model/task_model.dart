import 'package:flutter/material.dart';

class Task {
  final String buttonText;
  final String text;
  final int rangeCounter;

  Task({
    required this.buttonText,
    required this.text,
    required this.rangeCounter,
  });
}